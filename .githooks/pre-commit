#!/bin/sh
# Opt-in pre-commit guard: `git config core.hooksPath .githooks`
# Runs the offline ground guards (typecheck, facilities-check, surface-check on both tiers).
# Skip once with `git commit --no-verify`.
set -e
cd "$(git rev-parse --show-toplevel)"
echo "pre-commit: pnpm check"
pnpm check
