/**
 * Outskirts furniture: solar farms, the circuit's perimeter fence, light poles, utility poles
 * with wires and guardrails along the public roads — deferred through `ctx.farField` in the
 * 'dressing' stage, everything ≥ 140 m from the centreline on `ground.standY`. Stub: C2b.
 */
import type { EnvBuildContext } from './environment'

export function buildOutskirts(_ctx: EnvBuildContext): void {}
