/**
 * Parked cars in the OSM car parks (SUR_PARKING): low-poly bodies to 500 m, baked impostor
 * cards on the high tier, and a vertex-coloured "speckle" for the overview — all standing on
 * `ground.standY`, deferred through `ctx.farField` ('buildings' stage). Stub: filled in by C2b.
 */
import type { EnvBuildContext } from './environment'

export function buildParkedCars(_ctx: EnvBuildContext): void {}
