import type { EnvBuildContext } from './environment'

/**
 * The pit lane (plan I1-b): the lane's cross-section markings between the wall and the work
 * area, the garage-front equipment and the pit-wall furniture. Phase I0 lands the entry point
 * only — the umbrella (infield.ts) calls it in order and laps its wall-clock as 'pitLane'; the
 * content arrives with I1.
 */
export function buildPitLane(_ctx: EnvBuildContext): void {}
