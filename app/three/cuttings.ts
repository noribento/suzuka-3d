import type { EnvBuildContext } from './environment'

/**
 * The cuttings and tunnels (plan I6, P8): the prefectural road's cuttings, the works tunnel
 * under the pit straight, the 200R service tunnel, the pedestrian tunnels and the footbridges —
 * their walls, portals and parapets over the CUT corridors of the height field. Phase I0 lands
 * the entry point only — the umbrella (infield.ts) calls it in order and laps its wall-clock
 * into 'infield' together with the infield ground; the content arrives with I6.
 */
export function buildCuttings(_ctx: EnvBuildContext): void {}
